<div
    class="container-fluid contenu col-md-10">

    {# -- formulaire recette -- #}
    {{ form_start(form) }}

    <div class="row">
        <div class="col-md-5">{{ form_row(form.nom) }}</div>
        <div class="col-md-2"></div>
        <div class="col-md-5 col-form-label">{{ form_row(form.produits) }}</div>
    </div>

    <div class="row">
        <div>{{ form_rest(form) }}</div>
    </div>

    <div class="row">
        <div class="col-md-3"></div>
        <div class="col-md-2">
            <button class="btn btn-primary btn-xs">
                <i class="fa fa-save"></i>
                {{ button|default('Enregistrer') }}</button>
        </div>
        <div class="col-md-2">
            <button class="btn btn-info btn-xs" type="reset" title="Effacer la saisie">
                <i class="fa fa-eraser">
                    Annuler</i>
            </button>
        </div>
        <div class="col-md-2">
            <a href="{{ path('app_recette')}}" class="btn btn-secondary btn-xs">
                <i class="fa fa-undo"></i>
                Retour liste</a>
        </span>
    </div>
    {{ form_end(form) }}
</div>